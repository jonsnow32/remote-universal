{
  "name": "@remote/device-sdk",
  "version": "0.1.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "lint": "eslint src --ext .ts,.tsx",
    "test": "jest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@remote/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
